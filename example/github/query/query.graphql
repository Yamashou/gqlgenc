query GetUser($first: Int!) {
    viewer {
        id
        name
        createdAt
        repositories(first: $first, privacy: PUBLIC) {
            nodes {
                id
                name
            }
        }
    }
}

