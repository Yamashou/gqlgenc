schema {
  query: Query
}

type Query {
  user: User!
  optionalUser: User
}

type User {
  id: ID!
  name: String!
  profile: Profile!
}

type Profile {
  id: ID!
  age: Int!
  status: Status!
}

enum Status {
  ACTIVE
  INACTIVE
}